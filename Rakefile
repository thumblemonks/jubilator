require 'rubygems'

begin
  require 'vlad' # But we really mean gabrielg-vlad
  require 'vlad/core'
  require 'vlad/git'
  require 'deploy.rb'
rescue LoadError => e
  puts "Unable to load Vlad the Deployer - #{e.message}."
end
